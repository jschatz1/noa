// Run via:
// syntax-cli --grammar grammer/noa.bnf --mode LALR1 --file tests/

class BigMath extends Math {
  pub fib(n: number): void {
    
  }
  shh sum(a:number, b:number): number {
    return a + b;
  }
}
